<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.takatutustudio.Donasi">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".KonfirmasiActivity"></activity>
        <activity android:name=".MetodePembayaran" />
        <activity android:name=".shodaqoh" />
        <activity android:name=".MyTicketDetailAct" />
        <activity android:name=".MyProfileAct" />
        <activity android:name=".SuccessDonate" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SuccessRegisterAct" />
        <activity android:name=".SignInAct" />
        <activity android:name=".SplashAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>